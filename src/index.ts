import express from "express";
import Express from "./api/loaders/Express";

const app = express();

(async function () {
    Express;
});

app.listen(3030, () => {
    console.log("start Server");
});
